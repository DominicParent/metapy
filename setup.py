from setuptools import setup, find_packages

VERSION = '0.1.0'
DESCRIPTION = 'Python-based metadata management tool.'

setup(
    name="metapy",
    version=VERSION,
    description=DESCRIPTION,
    packages=find_packages(),
    install_requires=[]
)
